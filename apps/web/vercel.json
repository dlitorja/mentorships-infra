{
  // Skip preview builds for legacy web app; keep production builds
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = \"preview\" ]; then echo 'Skipping preview build for apps/web'; exit 0; else exit 1; fi"
}
